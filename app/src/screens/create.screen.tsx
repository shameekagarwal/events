import React from "react";
import { CreateTabNavigation } from "../navigation/create-tab.navigation";

// TODO:
// now this will house the form contextProvider and the top nav bar component
export const CreateScreen = () => <CreateTabNavigation />;
